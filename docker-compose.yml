version: "3.7"

services:
  prod:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: frontend-repository-procesos-politicas-contabilidad
    container_name: procesos-politicas-contabilidad-docker-nextjs-1
    ports:
      - "3006:3000"
    env_file:
      - .env
    networks:
      my_new_network:
        ipv4_address: 10.3.10.10  # Cambias la IP aquí

networks:
  my_new_network:
    driver: bridge
    ipam:
      config:
        - subnet: 10.3.0.0/16  # Mantienes esta subred
